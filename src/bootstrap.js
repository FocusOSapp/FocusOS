// Bootstrap module to load focused enhancements after the main app loads
// This simply imports the enhancement modules so they execute in the global app context
export {};
import './ambient.js';
import './monkMode.js';
import './blocker.js';
